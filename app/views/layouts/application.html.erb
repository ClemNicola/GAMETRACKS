<!DOCTYPE html>
<html>
  <head>
    <title>Gametracks</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <link href="https://fonts.googleapis.com/css2?family=Archivo+Black&family=Hind+Guntur:wght@300;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path('gtlogo.png') %>
  </head>

  <body>
    <%= render "shared/navbar" unless @skip_navbar %>
    <%= render "shared/flashes" %>
    <%= render 'shared/ios_pwa_links' %>

    <%= yield %>

    <%= render "shared/footer" unless @skip_footer %>
  </body>
</html>
